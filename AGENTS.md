# Codex Installation Instructions

When a user asks Codex to install this repository, run `./scripts/install.sh` from the repository root. The script performs the complete build, installation, and launch flow. Verify that the `CodexMenuBar` process is running after installation.

Do not request administrator access. The application installs into `~/Applications`.
